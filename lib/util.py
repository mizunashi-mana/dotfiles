# 1. test -f target in real file path
#   1. real config path
#   2.
# 2. test -f target in rc target name
# 3. fail
#
# return [absolute_path]
# [config path, ...] if is dir
# [config path] if is file
def resolve_target(target):
  raise 'not implemented'

